-- Gui to Lua
-- Version: 3.2

-- Instances:

local ScreenGui = Instance.new("ScreenGui")
local Frame = Instance.new("Frame")
local UICorner = Instance.new("UICorner")
local ON = Instance.new("TextButton")
local UICorner_2 = Instance.new("UICorner")
local OFF = Instance.new("TextButton")
local UICorner_3 = Instance.new("UICorner")

--Properties:

ScreenGui.Parent = game.Players.LocalPlayer:WaitForChild("PlayerGui")
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

Frame.Parent = ScreenGui
Frame.BackgroundColor3 = Color3.fromRGB(75, 75, 75)
Frame.BorderColor3 = Color3.fromRGB(0, 0, 0)
Frame.BorderSizePixel = 0
Frame.Position = UDim2.new(0.446139187, 0, 0.454787195, 0)
Frame.Size = UDim2.new(0, 113, 0, 62)

UICorner.Parent = Frame

ON.Name = "ON"
ON.Parent = Frame
ON.BackgroundColor3 = Color3.fromRGB(0, 84, 6)
ON.BorderColor3 = Color3.fromRGB(0, 0, 0)
ON.BorderSizePixel = 0
ON.Position = UDim2.new(0.0751809999, 0, 0.096215032, 0)
ON.Size = UDim2.new(0, 45, 0, 50)
ON.Font = Enum.Font.SourceSans
ON.Text = "ON"
ON.TextColor3 = Color3.fromRGB(255, 255, 255)
ON.TextSize = 27.000

UICorner_2.Parent = ON

OFF.Name = "OFF"
OFF.Parent = Frame
OFF.BackgroundColor3 = Color3.fromRGB(84, 0, 1)
OFF.BorderColor3 = Color3.fromRGB(0, 0, 0)
OFF.BorderSizePixel = 0
OFF.Position = UDim2.new(0.526582181, 0, 0.0919317454, 0)
OFF.Size = UDim2.new(0, 45, 0, 50)
OFF.Font = Enum.Font.SourceSans
OFF.Text = "OFF"
OFF.TextColor3 = Color3.fromRGB(255, 255, 255)
OFF.TextSize = 27.000

UICorner_3.Parent = OFF

-- Scripts:

local function GKEVH_fake_script() -- ON.LocalScript 
	local script = Instance.new('LocalScript', ON)

	local player = game.Workspace.Dunimo113 --ur usname
	script.Parent.Activated:Connect(function()
		while wait(0.001) do
		player.HumanoidRootPart:PivotTo(player:GetPivot() * CFrame.new(0, 0, -10))
		wait(0.001)
		player.HumanoidRootPart:PivotTo(player:GetPivot() * CFrame.new(0, 0, 10))
		wait(0.001)
		player.HumanoidRootPart:PivotTo(player:GetPivot() * CFrame.new(10, 0, 0))
		wait(0.001)
		player.HumanoidRootPart:PivotTo(player:GetPivot() * CFrame.new(-10, 0, 0))
		end
	end)
	
	
	
end
coroutine.wrap(GKEVH_fake_script)()
local function OMHSUS_fake_script() -- OFF.LocalScript 
	local script = Instance.new('LocalScript', OFF)

	local player = game.Workspace.Dunimo113-- ur usname
	
	script.Parent.Activated:Connect(function()
	player.Humanoid.Health = 0
	end)
end
coroutine.wrap(OMHSUS_fake_script)()
local function POYTA_fake_script() -- Frame.drag script 
	local script = Instance.new('LocalScript', Frame)

	local UIS = game:GetService("UserInputService")
	
	local frame = script.Parent
	local dragging = false
	local dragStart = nil
	local startPos = nil
	
	frame.InputBegan:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 then
			dragging = true
			dragStart = input.Position
			startPos = frame.Position
		end
	end)
	
	frame.InputEnded:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 then
			dragging = false
		end	
	end)
	
	UIS.InputChanged:Connect(function(input)
		if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
			local delta = input.Position - dragStart
			frame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
		end
	end)
end
coroutine.wrap(POYTA_fake_script)()
